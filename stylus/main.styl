@require 'leftSidebar/leftSidebar.styl'
@require 'mainContent/mainContent.styl'
@require 'header/header.styl'
@require 'employee/*.styl'
@require 'elder/*.styl'

.u-disabled {
  display: none;
}

.u-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(1, 1, 1, 0.5);

}

.paginationView {
  display: flex;
  justify-content: space-between;
}

.text-center {
  text-align: center;
}

.col-fit {
  width: 1%;
  white-space: nowrap;
}